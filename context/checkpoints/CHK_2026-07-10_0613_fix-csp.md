# CHK_2026-07-10_0613_fix-csp — Cierre FIX-20260710-03

**Fecha:** 2026-07-10 06:13 (Europe/Madrid)
**Sprint:** `FIX-20260710-03` — Content-Security-Policy + HSTS
**SPEC:** [`context/SPECs/SPEC-DEPLOY-001-csp-headers.md`](../SPECs/SPEC-DEPLOY-001-csp-headers.md)
**Agente implementador:** SOFIA (interrumpida, validado por INTEGRA)
**Estado:** ✅ Implementación completa, pendiente deploy y verificación en producción

---

## ✅ Lo que se hizo

### Cambios en `vercel.json` (deploy activo)

Añadidos 2 headers al bloque existente `/(.*)`:
```json
{ "key": "Content-Security-Policy", "value": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self' https://detailinghouse-api-production.up.railway.app; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests" },
{ "key": "Strict-Transport-Security", "value": "max-age=31536000; includeSubDomains" }
```

### Cambios en `netlify.toml` (por completitud, no en uso)

Añadidos los mismos 2 headers al bloque `[[headers]]` con `for = "/*"`.

## ✅ Validaciones (10/10 automatizables)

| ID | Criterio | Resultado |
|---|---|---|
| CA-1 | CSP en `vercel.json` | ✅ 1 ocurrencia |
| CA-2 | HSTS en `vercel.json` | ✅ 1 ocurrencia |
| CA-3 | CSP en `netlify.toml` | ✅ 1 ocurrencia |
| CA-4 | `default-src 'self'` | ✅ 1 ocurrencia |
| CA-5 | `script-src ... 'unsafe-inline'` | ✅ 1 ocurrencia |
| CA-6 | `fonts.googleapis.com` | ✅ 1 ocurrencia |
| CA-7 | `railway.app` en `connect-src` | ✅ 2 ocurrencias (en CSP + otra) |
| CA-8 | `frame-ancestors 'none'` | ✅ 1 ocurrencia |
| CA-9 | JSON válido | ✅ `python3 -c "import json; json.load(open('vercel.json'))"` OK |
| CA-10 | TOML válido | ✅ Sintaxis verificada |

## ⏸️ Validaciones pendientes (requieren deploy)

| ID | Criterio | Estado |
|---|---|---|
| CA-11 | Header CSP visible en producción | ⏳ Pendiente tras deploy |
| CA-12 | Sitio carga sin errores CSP en console | ⏳ Pendiente Playwright post-deploy |
| CA-13 | Google Fonts carga correctamente | ⏳ Pendiente verificación visual |
| CA-14 | Login admin sigue funcionando | ⏳ Pendiente Playwright post-deploy |

## ⚠️ Limitaciones conocidas (documentadas en SPEC §3)

- **`script-src 'unsafe-inline'`** es necesario porque todo el JS de `index.html` es inline. Eliminarlo requeriría refactor mayor (mover a nonces/hashes). Trade-off aceptado.
- **`'unsafe-inline'` en `style-src`** también es necesario por estilos inline.
- **COOP, COEP, CORP** no añadidos en este sprint (pueden romper features). Backlog futuro.

## ⏭️ Próximos pasos

1. Commit + push (SSH) — INTEGRA ejecuta
2. Vercel re-despliega automáticamente al detectar push
3. Verificar headers en `https://detailinghouse.com.mx` con `curl -sI`
4. Smoke E2E Playwright (sin errores de CSP en console)

---

**Sesión cerrada por:** INTEGRA (tras interrupción de SOFIA)
